import { connect, TYPOGRAPHY } from "@theme"

export default connect({
  container: "rounded-lg bg-blue-500 py-2 px-4 items-center justify-center",
  disabled: "bg-gray-300",
  title: `${TYPOGRAPHY.smallMedium} text-white text-center`,
})
